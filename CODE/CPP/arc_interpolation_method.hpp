#ifndef ARC_INTERPOLATION_METHOD_HPP
    #define ARC_INTERPOLATION_METHOD_HPP

    // -- TYPES

    enum class ArcInterpolationMethod
    {
        // -- CONSTANTS

        quadratic = 0,
        spherical = 1,
        count = 2,
        none = -1
    };
#endif
