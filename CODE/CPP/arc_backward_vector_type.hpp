#ifndef ARC_BACKWARD_VECTOR_TYPE_HPP
    #define ARC_BACKWARD_VECTOR_TYPE_HPP

    // -- TYPES

    enum class ArcBackwardVectorType
    {
        // -- CONSTANTS

        custom = 0,
        symmetrical = 1,
        residual = 2,
        count = 3,
        none = -1
    };
#endif
